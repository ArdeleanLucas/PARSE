{
  "name": "parse-desktop-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated Electron shell scaffold for PARSE desktop direction",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "node dev-launch.js",
    "test": "node --test \"test/**/*.test.js\"",
    "check": "node --check main.js && node --check preload.js && node --check dev-launch.js && node --check backend-supervisor.js && node --check backend-launcher.js && node --check project-store.js"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  }
}
