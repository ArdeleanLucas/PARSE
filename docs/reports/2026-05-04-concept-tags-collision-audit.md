# Concept Tags Collision Audit — 2026-05-04

## Result

**PASS: no existing top-level concept_tags fields found.**

No active annotation record, legacy annotation glob match, or annotation backup record scanned by this audit carries a top-level `concept_tags` key. This means the proposed `AnnotationRecord.concept_tags: Record<string, string[]>` sidecar has no observed field-name collision in the local PARSE workspace.

## Scope scanned

Audit root: `/home/lucas/parse-workspace`

| Bucket | Glob / source | Files scanned | JSON objects read | Files with existing tier sidecars (`confirmed_anchors` or `ipa_review`) | Files with top-level `concept_tags` |
|---|---|---:|---:|---:|---:|
| Requested legacy/sibling shape | `/home/lucas/parse-workspace/*/annotation*.json` plus sibling `annotation_*.json` | 0 | 0 | 0 | 0 |
| Active annotation records | `/home/lucas/parse-workspace/annotations/*.json` and `*.parse.json` | 20 | 20 | 12 | 0 |
| Annotation backups | `/home/lucas/parse-workspace/annotations/backups/**/*.json` excluding manifests/enrichments | 29 | 29 | 18 | 0 |
| **Total audited records** | union of the above | **49** | **49** | **30** | **0** |

Existing sidecar breakdown across all audited records:

- `confirmed_anchors`: 30 files
- `ipa_review`: 0 files

## Collision paths

- none

## Read errors / non-object JSON

- none

## Audit script

The report was generated by a read-only Python scan. It opened JSON files for inspection only and wrote this markdown report under `docs/reports/`.

```python
from pathlib import Path
import json
from collections import Counter

workspace = Path('/home/lucas/parse-workspace')
# requested legacy/sibling shape
for d in sorted(workspace.glob('*')):
    if d.is_dir():
        paths = list(d.glob('annotation*.json')) + list(d.glob('annotation_*.json'))
# active records and backups are also scanned because current PARSE stores records as
# /home/lucas/parse-workspace/annotations/<Speaker>.json and <Speaker>.parse.json.
```

Generated at: `2026-05-04T08:23:47+00:00`
