Flow: Hotkey / playback routing
Fixture: Saha01 single-speaker parity workspace

Oracle result
- Not executable. Oracle crashed before keyboard-routed annotate controls could be exercised.

Rebuild result
- Playback controls rendered, including `Play (Space)`, segment navigation, and speed controls.
- Cross-repo parity could not be exercised because oracle never reached the same state.

Status
FAIL

Why
Flow 01 blocks all downstream annotate interactions on the oracle baseline.
