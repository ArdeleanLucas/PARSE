Flow: Region capture / anchor offset
Fixture: Saha01 single-speaker parity workspace

Oracle result
- Not executable. Oracle crashed before timestamp tools could be exercised.

Rebuild result
- `Anchor offset here`, `Detect Timestamp Offset`, and `Detect offset (manual anchors)` controls were visible on the Saha01 annotate screen.
- Cross-repo parity could not be exercised because oracle never reached the same state.

Status
FAIL

Why
Flow 01 blocks all downstream annotate interactions on the oracle baseline.
